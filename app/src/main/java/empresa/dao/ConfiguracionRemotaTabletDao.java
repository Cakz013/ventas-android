package empresa.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import empresa.dao.ConfiguracionRemotaTablet;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CONFIGURACION_REMOTA_TABLET".
*/
public class ConfiguracionRemotaTabletDao extends AbstractDao<ConfiguracionRemotaTablet, Long> {

    public static final String TABLENAME = "CONFIGURACION_REMOTA_TABLET";

    /**
     * Properties of entity ConfiguracionRemotaTablet.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Idconfiguraciontablet = new Property(0, Long.class, "idconfiguraciontablet", true, "IDCONFIGURACIONTABLET");
        public final static Property Nombreconfig = new Property(1, String.class, "nombreconfig", false, "NOMBRECONFIG");
        public final static Property Valor = new Property(2, String.class, "valor", false, "VALOR");
    };


    public ConfiguracionRemotaTabletDao(DaoConfig config) {
        super(config);
    }

    public ConfiguracionRemotaTabletDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'CONFIGURACION_REMOTA_TABLET' (" + //
                "'IDCONFIGURACIONTABLET' INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: idconfiguraciontablet
                "'NOMBRECONFIG' TEXT NOT NULL UNIQUE ," + // 1: nombreconfig
                "'VALOR' TEXT NOT NULL );"); // 2: valor
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'CONFIGURACION_REMOTA_TABLET'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, ConfiguracionRemotaTablet entity) {
        stmt.clearBindings();

        Long idconfiguraciontablet = entity.getIdconfiguraciontablet();
        if (idconfiguraciontablet != null) {
            stmt.bindLong(1, idconfiguraciontablet);
        }
        stmt.bindString(2, entity.getNombreconfig());
        stmt.bindString(3, entity.getValor());
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }

    /** @inheritdoc */
    @Override
    public ConfiguracionRemotaTablet readEntity(Cursor cursor, int offset) {
        ConfiguracionRemotaTablet entity = new ConfiguracionRemotaTablet( //
                cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // idconfiguraciontablet
                cursor.getString(offset + 1), // nombreconfig
                cursor.getString(offset + 2) // valor
        );
        return entity;
    }

    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, ConfiguracionRemotaTablet entity, int offset) {
        entity.setIdconfiguraciontablet(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setNombreconfig(cursor.getString(offset + 1));
        entity.setValor(cursor.getString(offset + 2));
    }

    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(ConfiguracionRemotaTablet entity, long rowId) {
        entity.setIdconfiguraciontablet(rowId);
        return rowId;
    }

    /** @inheritdoc */
    @Override
    public Long getKey(ConfiguracionRemotaTablet entity) {
        if(entity != null) {
            return entity.getIdconfiguraciontablet();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override
    protected boolean isEntityUpdateable() {
        return true;
    }

}
