package empresa.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import empresa.dao.Barrio;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "BARRIO".
*/
public class BarrioDao extends AbstractDao<Barrio, Long> {

    public static final String TABLENAME = "BARRIO";

    /**
     * Properties of entity Barrio.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Idbarrio = new Property(0, Long.class, "idbarrio", true, "IDBARRIO");
        public final static Property Descripcion = new Property(1, String.class, "descripcion", false, "DESCRIPCION");
        public final static Property Idlocalidad = new Property(2, Long.class, "idlocalidad", false, "IDLOCALIDAD");
    };


    public BarrioDao(DaoConfig config) {
        super(config);
    }

    public BarrioDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'BARRIO' (" + //
                "'IDBARRIO' INTEGER PRIMARY KEY ," + // 0: idbarrio
                "'DESCRIPCION' TEXT NOT NULL ," + // 1: descripcion
                "'IDLOCALIDAD' INTEGER);"); // 2: idlocalidad
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'BARRIO'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, Barrio entity) {
        stmt.clearBindings();

        Long idbarrio = entity.getIdbarrio();
        if (idbarrio != null) {
            stmt.bindLong(1, idbarrio);
        }
        stmt.bindString(2, entity.getDescripcion());

        Long idlocalidad = entity.getIdlocalidad();
        if (idlocalidad != null) {
            stmt.bindLong(3, idlocalidad);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }

    /** @inheritdoc */
    @Override
    public Barrio readEntity(Cursor cursor, int offset) {
        Barrio entity = new Barrio( //
                cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // idbarrio
                cursor.getString(offset + 1), // descripcion
                cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2) // idlocalidad
        );
        return entity;
    }

    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, Barrio entity, int offset) {
        entity.setIdbarrio(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setDescripcion(cursor.getString(offset + 1));
        entity.setIdlocalidad(cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2));
    }

    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(Barrio entity, long rowId) {
        entity.setIdbarrio(rowId);
        return rowId;
    }

    /** @inheritdoc */
    @Override
    public Long getKey(Barrio entity) {
        if(entity != null) {
            return entity.getIdbarrio();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override
    protected boolean isEntityUpdateable() {
        return true;
    }

}
